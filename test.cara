var fib_numbers = [];

fn fib(n) {
    append(fib_numbers,1);
    append(fib_numbers,1);

    var i = 2;

    while i < n {
        append(fib_numbers,fib_numbers[i-1] + fib_numbers[i-2]);
        i = i+1;
    }
}

fib(1 << 1*2 + 1);

print(fib_numbers);



