var fib_numbers = [];

fn fib(n) {
    append(fib_numbers,1);
    append(fib_numbers,1);

    for i in (2,n) {
        append(fib_numbers,fib_numbers[i-1] + fib_numbers[i-2]);
    }
}

if 0 < 1 && 1 < 2 && 2 <= 3 || 1 < 0 {
    print(10);
}

fib(10000);



